# Assumption Register

> Status: **live.** Every falsifiable belief this product is built on that is not backed by
> evidence the project holds. One row per assumption, each with a named validation method.
>
> Created 2026-07-26, discharging a mandate open since Phase 1: *"decide on product reasoning,
> label it **Product judgement** or **Assumption**, and enter it in the Assumption Register with a
> named Validation Method"* (`docs/superpowers/research/foundation-audit-p1/P1-gate-summary.md:252-253`).
> The register was specified at
> `docs/superpowers/specs/2026-07-17-adaptive-learning-foundation-audit-research.md:281-286` and had
> never been built. `2026-07-22-product-design-inputs.md:8-9` claimed to contain it and did not.

## The rule

**Prose labels return nobody to anything.** "This is a product judgement until calibrated" written
inside a design document is invisible the moment the document is filed. A row here is a standing
obligation: when its validation method runs, someone has to come back and settle it.

Three things follow.

1. **Every threshold, interval, count, and duration this product ships gets a row** until it is
   calibrated on this product's own data. The archive holds no threshold constant, and the
   activity baseline states outright that optimal intervals *"cannot be copied into product
   configuration."*
2. **A validation method must name its mode** — `playtesting`, `academic research`, or
   `production telemetry`. "We'll see" is not a method. The charter offers a fourth,
   "future experimentation"; this register deliberately does not use it, because it is the
   "we'll see" this rule forbids wearing a better coat.
3. **A row is closed by evidence, not by time passing.** Closing one means writing what was
   measured, where, and what it changed. Rows are struck, never deleted.

## Distinction that governs which beliefs land here

The Phase 2 audit separated two things that read identically in a document:

- **Product judgement** — a values or design choice. Free to change; needs a label, not a row here.
  Example: which subjects precede which (`K-U7-008`).
- **Assumption** — falsifiable, untested, and would change under contradicting data. **Needs a row.**
  Example: the 7-stage skill order (`K-U1-003`).

Full split: `docs/superpowers/research/evidence-index/P2-verdict-catalog.md` §Bottom line.

---

## Register

| # | Assumption | Evidence held | Confidence | Validation method |
|---|---|---|---|---|
| A-01 | ~~The 7-stage skill order (game literacy → rule literacy → chart literacy → decision recall → procedural transfer → ruleset transfer → automaticity) is the right prerequisite ordering.~~ `K-U1-003` **WITHDRAWN, not validated** — 2026-08-01 at `LDB-01`. | None, and none was ever gathered. **This row is not closed by evidence; the assertion was withdrawn.** `LDB-01` found the list mixes two kinds of thing: stages 1–3 name content (absorbed into the Subject partition) and stages 4–7 name conditions of performance (they became the support / scope / ruleset / pace Condition axes). No seven-step prerequisite ordering is asserted by any live document. Residue: `A-02`. | — | **None required** — nothing now claims this ordering. If a future design re-asserts a fixed stage order, open a new row; do not reuse this one. Spec: `2026-08-01-learning-outcomes-and-skill-graph.md` §4. |
| A-02 | Accuracy must be stable before speed is trained. `K-U1-004` **Inherited, not re-asserted** — `LDB-01` (2026-08-01) makes pace a Condition axis and claims **no** ordering against it; support is the only axis asserted as primary, on an owner decision. This row is the residue of `A-01`. | **The weakest-evidence claim in the audit.** Its only blackjack-specific support is N=12 with computer-controlled win probability and no split, double, or insurance. Note also a silent loosening: the import said "after **mastery**"; the spec says "after accuracy is stable." | Very low | **playtesting** — compare a timed-earlier arm against a timed-later arm on retained decision accuracy. |
| A-03 | Immediate correction suits acquisition and delayed review suits transfer. `K-U1-014`, `ALR-005` | Runs against its own cited source: the meta-analysis reports **delayed** feedback as slightly more effective and says *"clarity and consistency … is more essential than the actual timing."* Its own meta-regression found timing did not contribute significantly. Logged as `CFL-004`, open. | Low | **playtesting** — the honest test is consistency versus timing, not immediate versus delayed. |
| A-04 | The specific rungs of the fading ladder (table open → hidden → hidden at pace) and of the four-level hint ladder are the right ones. `K-U2-006` | The *principle* — fade assistance before counting evidence as independent — is evidence-backed. The specific rungs are not; the baseline calls the existing ladder *"a project design rather than a research-derived sequence."* | Medium on the principle, low on the rungs | **playtesting** — measure whether performance survives each rung removal. |
| A-05 | A chess-style internal rating can run alongside deterministic mastery gates. `K-U1-016` | Each half has a precedent; **the combination has none.** Hidden dependency: education-Elo needs roughly 100 learners for usable item difficulty, and Chess.com seeds new item difficulty by observing who solves it. A single-learner product has neither. | Low | **production telemetry**, and not before a user base exists. Phase 1 found the requirement shrinks to a single published constant with per-learner online fitting — that is the path to evaluate, not Chess.com's seeding. Tracked as `E-6` in the economy spec. |
| A-06 | Mastery should decay to `Review due` as retention evidence ages. `K-U3-007` | Spacing transfer to a blackjack-shaped decision rule is a **settled** coverage gap — four independent confirmations that no such study exists. The only product analogue (Khan) has **no time-based decay at all**. | Low | **production telemetry** — longitudinal; needs returning learners over weeks. |
| A-07 | Every numeric threshold, retention interval, evidence count, spacing schedule, and session duration the product ships. `K-U3-001`, `K-U3-002`, `ALR-027`, `ALR-032` | **The archive holds no threshold constant.** The phrase "research-calibrated" asserts a property nothing in this project possesses; do not write it. It was struck from every live document on 2026-07-26 — audit records that quote the defect are process history and stay as they are. | None — these are invented until measured | **production telemetry** — every constant is provisional on this product's own attempt data. Each specific constant chosen at `LDB-04`/`LDB-06` inherits this row; add a sub-row when a number is actually written. |
| A-07a | **Sub-row of `A-07`.** `k = 8` and `n = 10` — a Skill is mastered at **8 of the last 10** table-closed presentations, scored on first response. `LDB-04` (approved 2026-08-03), spec `2026-08-03-evidence-and-mastery-rules.md` D13. **The first sub-row filed under `A-07`; it sets the convention its parent row asks for.** | None. Both numbers are invented, as `A-07` requires every constant to be. Three stated grounds, none of them evidence: 80% sits far above any guessing rate; the window still fills for a Skill as rare as `natural-blackjack` (~4.8%, roughly 200 hands); and *"8 of your last 10"* is legible in a way *"16 of your last 20"* is not — a **design** reason, labelled as one. The two are pooled rather than split because moving `n` changes what `k` means. | None — invented until measured | **production telemetry**. **Named first test:** do learners who clear 8 of 10 then fail on organic play? Computable from stored `ProgressAttempt` records with no new instrument, because `k` and `n` are reducer config rather than constants baked in — the same stored attempts can be replayed re-parameterised. |
| A-07b | **Sub-row of `A-07`.** The stake constants — per table tier, the **minimum bet**, the **minimum buy-in** and the **maximum buy-in**; and how many tiers exist. `LDB-05` D2 (approved 2026-08-05). Nothing in the engine holds these today: `Ruleset` (`crates/blackjack-core/src/types.rs:63-74`) declares no bet limit of any kind, and `validate_bet` (`session.rs:499-507`) enforces only `bet > 0` and the 3:2 even-units rule — both read first-hand 2026-08-05. | None. Every number is invented, as `A-07` requires. | None — invented until measured | **production telemetry.** **Named first test:** what fraction of sessions end by busting the stack within N hands? A buy-in that busts too fast makes the table feel punitive; one that never busts makes it feel weightless. **Second test:** how often does a learner fall below the *lowest* tier's minimum buy-in — the `LDB-05` D3 setback boundary, which is where Free Play closes — and does hitting it read as a prompt to learn or as a wall? |
| A-07c | **Sub-row of `A-07`.** The pay rates — the full rate per `Completion` and per `Review due` clear, and the reduced **trickle** for practice on already-mastered material. `LDB-05` D5 (approved 2026-08-05). | None. Invented. **This row carries more weight than its pre-gate draft did:** the 2026-08-05 gate dropped the per-window cap, so the trickle rate is now the **only** thing bounding how fast a learner can refill a wallet. One constant is doing the job two were sharing. | None — invented until measured | **production telemetry.** **Named first test:** measured wallet trajectories — how many minutes of learning fund one session, and does the trickle alone keep a fully-mastered learner solvent? The second half is the direct test of `LDB-05` D3's guarantee that no reachable state leaves a learner unable to earn. |
| A-07e | **Sub-row of `A-07`.** The three session-size presets — **Short 10, Standard 25, Long 50** Presentations. `LDB-06` D7 (approved 2026-08-17). Pooled, because moving one preset changes what the others mean — the convention `A-07a` set. | None. Invented. | None — invented until measured | **playtesting.** **Named first test:** completion rate and return rate by preset — `A-08`'s existing instrument at no extra cost, which is why the mode matches `A-08`'s. **⚠ Blocked until `LDB-06` §11 item 7 lands (`RC-07`):** the preset survives only on a closed session (D17), so as things stand the test can see nothing but sessions that completed — and completion is what it measures. The row stands; its named method does not run until `presetId` reaches the attempt. |
| A-07f | **Sub-row of `A-07`.** The Recommender's **separation gap** — a missed item may return after at least **3** intervening Presentations. `LDB-06` D14 (approved 2026-08-17). | None. Invented. | None — invented until measured | **production telemetry.** **Named first test:** does a shorter gap raise same-session accuracy while lowering next-session accuracy? Computable from stored attempts with no new instrument. |
| A-08 | "Short learning loops, like Duolingo" describes a workable session length. `K-U2-002` | A live conflict: Duolingo says a few minutes, Brilliant recommends fifteen with a two-minute option. The baseline grades the topic *"Medium-low; durations are product recommendations, not comparative causal thresholds."* | Low | **playtesting** — learner-selected duration with completion and return rates recorded. |
| A-09 | A read-only skill decays if its first application is several lessons away. `K-U7-006` | The configuration is a coverage gap; no source measures a taught procedural skill decaying from delayed first application. | Low | **playtesting** — vary the gap between teaching and first application. |
| A-10 | ~~Multiple choice is a valid format for mastery evidence.~~ `K-U4-003` **RETIRED, as this row predicted** — 2026-08-03 at `LDB-04`. | **None of the three sources cited for it addresses format validity.** It governed 100% of shipped mastery evidence, and the bridge separately ruled that a mastery model which cannot ingest played-hand evidence is disqualified. **`LDB-04` replaced the format rather than validating it:** `deal-and-decide` is `primaryFor` all eight decision Skills and, under D6, is the **only** route to those bars; its `provenance` is `["organic","arranged"]`, so it cannot be posed. Multiple choice can no longer reach a decision Skill's mastery bar at all. | — | **None required** — nothing now claims this. If a future design admits multiple choice as primary mastery evidence, open a new row; do not reuse this one. Spec: `2026-08-03-evidence-and-mastery-rules.md` §10, D6. |
| A-11 | Two-to-four-minute units are the right size. `K-U7-001` | No citation. `ALR-027`: *"the research does not establish universal minutes."* | Low | **playtesting** — see A-08; same instrument. |
| A-12 | The checkpoint-hybrid generation boundary limits token cost and latency. `K-U5-008` | The mechanism exists; **no measurement backs the claim.** The same design files token, latency, and cost as work still to be done. | Unknown | **production telemetry** — measure it or drop the claim. |
| A-13 | Learners can be trained to evaluate a decision independently of the hand result. | **Load-bearing and open.** The product is committed to it and it is implemented. Outcome bias is well demonstrated — pre-registered, N=692, reproduced at a larger effect, and present even among people who had said outcomes should not count. **Correcting** it is not demonstrated. The one dedicated intervention is abstract-only. | Unknown, and the product depends on it | **playtesting — `P-1`.** Behavioural, not attitudinal: agreeing with the principle is worthless as a measure. |
| A-14 | Teaching expected value changes how a learner plays. | **Evidenced absence.** Two independent agents, multiple routes, found no study measuring whether EV instruction improves in-game decisions. The nearest handbook review predicts Bayes-like rules to be poor training candidates. Further collection is **not authorised**. | Unknown | **DORMANT** — 2026-08-01 at `LDB-01`. `P-2` is declared **out** of the phase-5 subset because EV is not taught explicitly: it gets no Skill and appears only as rationale in feedback copy, so no EV-instructed arm exists to measure. Not closed — choosing not to teach something is not a measurement. **Reopening condition:** insurance is the one in-scope decision the strategy table does not cover, and it is removed from the decision space by `insurance_auto_decline: true` (`types.rs:73`, `rules.rs:25,29-34`, `session.rs:101-104`, `web/src/learn/engine.ts:30`) — a config flag, not a law. If insurance is ever surfaced as teachable, this row wakes and `P-2` returns. Original method, held for that case: play-decision accuracy across an EV-instructed and a non-EV arm, measured on hands played, not quiz items. |
| A-15 | Mixed practice helps this audience rather than overwhelming beginners. | The interleaving result is strong (72% vs 38%, d=1.05, discrimination errors 46% → 10% — the pair is stated together at **P2 Bottom line A.2**, `docs/superpowers/research/evidence-index/P2-verdict-catalog.md:198`; `product-design-inputs` §1.1 carries only the 72/38 half) but is grade-7 mathematics, n=140. Adult blackjack transfer is untested. The `CFL-007` ruling — block to introduce, interleave to practise — is explicitly labelled a bet. | Medium on mechanism, unknown on transfer | **playtesting — `P-3`.** Classification accuracy plus beginner drop-off, with a retention arm: blocked practice typically wins in-session and loses on delay. |
| A-16 | A deliberately simplified false heuristic ("assume the next card is a ten") would not serve novices better than correct strategy. | The evidence points the other way: casino players use exactly that heuristic, it is easier to learn than optimal strategy, and it is associated with better expected returns ~~than unaided play~~ **— comparator NOT stated by the source; see the correction note in this row**. The bridge calls adopting it *"a genuine design option, not a concession."* | Unknown | **REWRITTEN** — 2026-08-01 at `LDB-01`. `P-4` as written required an arm that *plays* the heuristic; no such arm will be built, because the heuristic implies Stand where the shipped table says Hit on **27 of 50 hard-12–16 cells** (identical H17/S17, including hard 16 vs 10), and a policy the oracle punishes across half the decision space needs a second grading authority that bridge §5 forbids. The heuristic is adopted as an **explanatory frame only**. `P-4` is declared out of the phase-5 subset. **New method — playtesting:** one feedback-copy variant that explains dealer bust risk via the ten-assumption, measured on stiff-hand decision accuracy. ✅ **COMPARATOR RESOLVED 2026-08-17 at `LDB-10`.** The independent re-check ran and *"than unaided play"* is **struck at all three loci together** — this row, bridge §1.8, and the §1 table row in `P3-evidence-catalog.md`. The publisher abstract states the claim bare (*"its use is associated with better expected returns"*); its only comparator is bonded to ease of learning. Positive absence enumeration: `unaided`, `compar*`, `versus`, `control`, `outperform`, `basic strategy` all occur **zero** times in the 244-word abstract and title. **This weakens the row's own counter-evidence rather than the assumption:** the two adjacent sources that do name a comparator name *players' actual hit–stand play*, and on the author's blog arithmetic the heuristic is ~0.04% **worse** than perfect basic strategy — which points *toward* this assumption, not away from it. Both figures are labelled leads from non-paper sources and are **not** promoted to Evidence-held; F20 additionally carries the standing prohibition *"nothing here may be cited as a number"* (`C7-probability-ev-variance.md:1567`). Full text still unobtained (`oa_status: closed`, 18 routes). Record: `docs/superpowers/research/foundation-audit-p3/verification/V-A16-comparator.md` — inbox is evidence, never authority. **The validation method is unchanged**; the re-check corrected a citation, not the design ruling. |
| A-17 | Confidence and skill rise together, so a confident learner is a competent one. | The evidence says they will not: brief practice significantly raised confidence on an outcome the participant could not influence. | Low — expect divergence | **playtesting — `P-5`.** Paired longitudinal series of self-rated confidence and measured decision accuracy on the same sessions; the measurement is the divergence. |
| A-17a | **Sub-row of `A-17`.** A learner's **request** for a Challenge is a usable proxy for self-rated confidence. `LDB-11` D1 (approved 2026-08-22): the product never asks *"how confident are you?"* — it records that the learner asked, and what the Mastery window said at that moment (`LDB-11` D13 `windowStateAtDeclaration`). | **None held.** `A-17`'s own evidence concerns *self-rated* confidence, not the act of requesting a test of it; nothing in the archive measures whether asking tracks feeling ready. This proxy is the only instrument `P-5` has, because `D1` forbids the self-rating prompt `A-17`'s method presumed. | Low | **playtesting — `P-5`.** Compare the declaration series (learner-initiated requests, `initiatedBy: learner`, each with its window snapshot) against the Mastery window at declaration time; the divergence between *asked while the bar was not met* and *asked while it was* is the series. **Known limitation, recorded rather than hidden: this measures only learners who act on their confidence. A learner who feels ready and never asks is invisible.** |
| A-18 | An interactive learning game teaches these concepts better than passive presentation. | Encouraging on format, weak on content: a single ~60-minute debiasing game produced persistent effects, but it had **no untrained control arm**, the **video beat the game on bias knowledge**, and **none of its six trained biases** is outcome bias or EV reasoning. | Medium on format, none on content | **playtesting** — this is the premise behind the whole activity-pattern catalog (`LDB-02`); instrument the first non-quiz activity against a quiz baseline. |
| A-19 | Instruction can correct gambler's fallacy, hot-hand belief, and illusion of control. | Prevalence is strongly established, including in the field with real casino patrons betting their own money. Correction is thinly evidenced: one clinical treatment *package* that cannot isolate the randomness-correction component, and one lab study on imaginary money bundling teaching with in-play cueing. | Low | **academic research** — one bounded gap remains (`G5`: Steenbergh et al. 2004, never opened) — then **playtesting**. |
| A-20 | Chips returned on a won hand buy table time only, and buying table time does not train outcome bias. `LDB-05` D1 (approved 2026-08-05) | None. The mitigation is structural but **narrower than "cosmetic"** — the word this row carried until 2026-08-05, when `LDB-05` D1 made it false: winning returns **real** chips that buy **real** table time, and under D2 a larger wallet also opens higher **stake tiers**. What survives is that chips advance the learner on no axis — not a unit, not a Skill, not a rank, not a difficulty tier — and that a stake tier is not a difficulty tier, because the strategy chart is identical at every stake. But they do buy something the learner wants. Whether that still trains the bias is untested. | Unknown | **playtesting — shares the `P-1` instrument.** **Named test:** `adherence-under-loss` divergence across matched arranged and organic runs, comparing an arm where wins credit the wallet against one where they do not. Tracked as `E-1` in `2026-07-26-chips-xp-and-progression-economy.md`; answered by `2026-08-04-motivation-and-chips-economy.md` D1. |
| A-21 | Drawing the hit/stand frontier onto a blank chart grid measures whether the learner holds the *shape* of the strategy, not just recall of individual cells. | None held. The sourced pattern (U2-3, stroke tracing) has the learner trace a **displayed** glyph; the blank-grid version is this project's own extension and the source makes no measurement claim about either. Classified **cosmetic** on the sourced form. | Low | Playtesting — compare blank-grid frontier drawing against per-cell recall on the same learners; the sourced pattern measures neither |
| A-22 | A configurable variance sandbox — learner sets shoe, ruleset and bet ramp, then runs it — teaches that correct play still loses, rather than merely demonstrating it. | None held. U2-13's source describes an authoring risk, and the catalog row states plainly that neither source claims what it measures about the learner. The engine already supports the mechanic; what is unevidenced is that running it *changes belief*. | Low | Playtesting — measure belief in the decision/outcome split before and after sandbox use, against the misconception `how-to-teach.md` Part 8 item 12 names as the most important to defeat |
| A-23 | Practice on **arranged** Decision situations (a composition-honest opening placed on top of a real shoe, `shoe.rs:62`) produces the same Skill evidence as situations met in **organic** shoe order. `LDB-03` §5 | None held, and the one relevant result cuts against it. Interleaving's benefit is a **discrimination** benefit — it improves telling the kinds of problem apart: 72% vs 38%, d=1.05 (`product-design-inputs` §1.1 `[VERIFIED]`), and discrimination errors 46% vs 10% (**P2 Bottom line A.2**, `docs/superpowers/research/evidence-index/P2-verdict-catalog.md:198`; that figure is not in §1.1 and does not carry its `[VERIFIED]` tag). A learner drilled on arranged hard-16s has produced the correct action many times **without ever having to notice a hard 16 arise**, which is the half that result says is trained. Nothing in the archive measures arranged-vs-organic exposure directly. `LDB-03` rules arranged *is* dealt evidence for a Skill; whether arranged-only evidence can reach **mastery** is deliberately left open to `LDB-04`. | Low | **Playtesting** — an arranged-only arm against a mixed arm, tested on **organic** shoe play, scoring classification errors separately from action errors. The arranged arm should match on action selection and, if this row is wrong, lag on classification. `P-3` is the nearest scheduled instrument |
| A-24 | Per-learner online item fitting from one published default constant is sufficient for this product's rating, at n = 1 learner. `LDB-05` D10 (approved 2026-08-05) | **F26** (Sense, Meijer & van Rijn 2018, *Front Educ*, Q3) documents the exact architecture — *"start every item at a fixed default of 0.3, adapt online from the single learner"* — 4/4 quotes verbatim and shipped rather than proposed (`P1-evidence-catalog.md:54`). **F2** confirms the alternative is unavailable: Chess.com's puzzle rating *"is determined by who is able to solve it"* — crowd calibration, not population-free seeding (`:113`). **Two qualifications this row carries and the premise document omits, both from `:59`:** the architecture is population-***light*, not population-free** — the default constant is itself a number somebody derived from a population, and the head statement asserting the population requirement *falsified* was **struck as overstated**, with a verifier ruling it **not** falsified; and F17/F24/F25/F26 are **one lab (Groningen/van Rijn), one system (SlimStampen/MemoryLab), one commercial lineage** — *"four citations on C1's central question, previously undisclosed, and not four independent lines of evidence."* | Medium on the architecture; the sufficiency-at-n=1 claim is untested | **production telemetry.** **Named first test:** does the fitted per-item parameter stabilise within one learner's own response history, and does difficulty selection track measured accuracy? The decisive fact needs no citation: this product has one user. |
| A-26 | Grouping several Decision situations that share one action teaches the abstraction, rather than supplying the Classification for free. (`principle-name`, from U3-9.) `LDB-09` D2 | **Weak, and partly against it.** The sourced pattern is N = 20, one experiment, no replication; it measures later *memory* rather than abstraction, and its own authors call the experiment "contrived" — `classification.md` files it as the **weakest-evidenced substantive row in the catalog**. `LDB-03` rejected it for measurement precisely because grouping supplies the Classification the recognition rule forbids; this row records that the *teaching* claim is a separate question the rejection never reached. | Low | **Playtesting** — `principle-name` against per-cell recall on the same learners, scoring transfer to unseen cells rather than to the grouped ones. This is the admitted type whose evidence is thinnest, so it is the first of the five to test. |
| A-27 | Eliciting a point estimate *before* revealing the true value builds variance intuition better than showing the value. (`estimate-and-check`, from U2-4.) `LDB-09` D2 | **None held.** U2-4's source (a fraction number line with haptic feedback) makes no measurement claim of the kind, and `classification.md` classified the row **cosmetic** — a continuous widget over a question a selection widget asks equally well. `how-to-teach.md` Part 4's *"teach dealer bust rates BEFORE showing the chart"* is a **product premise, not evidence**, and is the only thing behind this. Distinct from `A-22`, which covers a *configurable sandbox* the learner runs, not an elicited estimate — checked rather than assumed. | Low | **Playtesting** against a show-the-number baseline: same quantities, one arm estimating first, both then shown the value, measured later on unshown quantities. |
| A-28 | Authoring a policy from fixed parts and watching it tested against generated hands teaches the policy. (`rule-battery`, from U2-10.) `LDB-09` D2 | **None held.** The sourced pattern is an exercise platform's test-suite workflow described by its own vendor; no source in the catalog measures whether authoring-then-testing teaches better than deciding hand by hand. `LDB-03` rejected the row on measurement economy — *"two instruments for one Skill; the cheaper one is already adopted"* — which says nothing about its teaching value, and the free-text form it costed is not the form admitted. | Low | **Playtesting.** Also the strongest instrument for closing `A-18`, whose method is *"instrument the first non-quiz activity against a quiz baseline"* — this is the admitted type furthest from a quiz. |
| A-29 | A **forgiving weekly streak** — free, earned, auto-applied forgiveness; a week counts on any committed Presentation; gain-framed copy — carries return-cadence motivation, which nothing in v1 does. `LDB-06` D8 / §12 divergence 6 (struck and replaced, 2026-08-17). **Deferred: not built in v1; trigger is the account.** | **Relayed, and mixed.** From `docs/superpowers/research/design-evidence/2026-08-17-streak-mechanics.md` (tracked 2026-08-17; **not authoritative, not verified**; **nobody in this repository has opened any of its 18 external sources**). *For:* the one large education RCT reporting **no discouragement effect** used a weekly, gain-framed streak; Khan Academy removed daily streaks in 2021 and returned in 2024 with a weekly one; Brilliant's protection is free, earned and auto-applied; and a habit study finds one missed day costs ~0.29 SRHI points with no long-term effect, so a weekly window forgives what does not matter. *Against:* in the only study separating the parts, an **intact** streak did **not** significantly beat showing no streak at all (p = .31), and its authors concede that contrast was inconsistent across studies — the reliably measurable effect is the **break**. **No study anywhere compares a never-falling counter with a resettable one**, which is what killed the up-only variant this replaces. | Low, and the direction of the risk is known: it may buy nothing | **production telemetry**, and not before accounts exist — the mechanic cannot be honest on per-device IndexedDB, which is why it is deferred rather than built. **Named first test:** return cadence — distinct active days per learner per fortnight — against a no-streak arm, with a second arm on window length. **Second test, because the failure mode is specific:** among learners whose streak lapses, does return rate fall *below* the no-streak arm? That is the harm the evidence actually establishes, and a forgiving design is a bet that widening the window avoids it rather than a proof that it does. |
| A-30 | **Units are ordered, and a learner advances one step at a time unless they clear a Unit Challenge on what they are skipping.** Owner decision 2026-08-17, on the reasoning that skipping should require demonstrated knowledge. *Reworded 2026-08-22 at the `LDB-11` gate: the skip-test this row presumed is now designed as the **Unit Challenge** (`LDB-11` D11), and its adequacy as skip evidence is its own row, `A-32`.* | **None, and none was sought — this is product judgement.** `LDB-01` §3 records that this project has **no citation for any of its orderings** (`K-U7-003`, `K-U7-007`), which is why it defined a Prerequisite as comprehension dependency and left sequencing to the product: *"those orderings are simply not edges; they are sequencing choices `LDB-06` makes, and a register row covers any that get asserted."* This is that assertion, and this is that row. `A-01`'s withdrawn seven-stage order closes with *"If a future design re-asserts a fixed stage order, open a new row; do not reuse this one"* — followed. | **n/a — product judgement, not a confidence claim** | **playtesting**, two measures. (1) Does the Unit Challenge work as skip evidence — carried by `A-32`, filed 2026-08-22, so each row holds one claim. (2) Does the lock cost what `LDB-01`'s *"never a lock"* was protecting: watch abandonment at a closed Unit against constraint 2. |
| A-31 | A two-part Challenge — a playful Coached warm-up of Unmeasured Activities, then a Proving run — motivates a learner to prove a Skill better than a Proving run alone. `LDB-11` §2, D17 (approved 2026-08-22; §10 divergence 2, accepted with the warm-up kept). | **None held, and the one relevant result cuts against it.** Brummer et al. (2024), as relayed at `2026-08-08-session-composition.md` D9: *"a combination of feedback timing approaches was ineffective"* — a warm-up that corrects immediately followed by a run whose feedback is deferred is such a combination, whichever way the product models it. `LDB-11` D17 narrows the threat structurally (the warm-up holds no dealt hands, so it cannot hand the run its answers) but does not answer the motivation claim. Nearest precedent is `A-22`, whose evidence is *"None held"*. For 12 of the 17 gated Skills the warm-up rehearses nothing relevant and is ceremony only (`LDB-11` D17). | Low, and the direction of the risk is known: it may buy nothing | **Playtesting** — clear-rate and request-rate with the warm-up against without. **Reopening condition already written at `LDB-11` §6:** if clear-rates for learners who take the warm-up come out at or below those who skip it, Brummer's finding has reproduced here and the warm-up moves out of the Challenge into ordinary Coached sessions. Computable from stored data with no new instrument. |
| A-32 | Clearing a Unit Challenge — coverage-complete over every gated Skill in the Unit (`ALR-034`), proved on dealt hands with no strategy table — is adequate evidence to skip the Units it covers. `LDB-11` D11 (approved 2026-08-22). This is `A-30`'s missing test, given its own row. | **None, and none was sought** — `A-30` records exactly that, and the Challenge was designed on product judgement rather than on evidence that a short arranged run predicts later organic play. `A-23` records the nearest open question: whether arranged-only evidence matches organic exposure on *classification*, which is the half a skipper never had to notice arise. | **n/a — product judgement** | **playtesting.** Track learners who skip a Unit by clearing its Challenge against learners who walk it, on later **organic** play of the skipped Unit's Skills, scoring classification errors separately from action errors (the `A-23` split). If the Challenge is doing its job the skippers should not lag. Computable from stored attempts once Units and the declaration record (`LDB-11` D13) exist. |

**`A-25` remains unused and is not reissued.** It was allocated to a dropped cap at `LDB-05` and is
recorded below as never filed; `LDB-09`'s three rows are numbered `A-26`–`A-28` so that record stays
readable rather than colliding with a reused id. `LDB-11`'s rows (2026-08-22) are `A-17a`, `A-31`, `A-32`.

**Never filed, and named so the absence is not read as an omission.** `A-07d` (a per-window practice
earnings cap — amount and window length) and `A-25` (that such a cap does not read as an energy gate)
were drafted in `2026-08-04-motivation-and-chips-economy.md` §10 and **were never written here**,
because the 2026-08-05 gate dropped the cap itself (§12 divergence 3). No row was retired; neither ever
existed. A future free-tier bound — the energy or error-rate mechanism recorded as deferred in
`journal/decisions.md` — will need its own rows and must be argued against `how-to-teach.md:122`.

## How to use this

- **Writing a design document:** if a claim would change under contradicting data, it belongs here.
  Cite the row (`A-07`) instead of restating the caveat in prose.
- **Shaping a card:** a card whose outcome rests on a row states which one, so the decision is
  visibly provisional.
- **Choosing the phase-5 slice** (`LDB-08`): the declared subset of `P-1`…`P-5` the slice must
  instrument determines which of `A-13` through `A-17` and `A-20` can close.
- **Closing a row:** strike it, record what was measured and where, and note what it changed. If
  nothing changed, say so — a validated assumption is a finding.
