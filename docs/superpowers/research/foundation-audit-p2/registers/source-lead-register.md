# Source-lead register

Agents return rows. IDs are assigned by the orchestrator. An agent that predicts an ID has made an
error.

<!-- Agents return a source lead (a document, figure, or specimen worth another pass's
     attention) as a row; the orchestrator assigns the SL-NNN id and appends it here. An agent
     that writes its own predicted id has made an error (G6) — same discipline as the other two
     registers. -->

| Lead ID | Path | What it is | Raised by / for |
|----|----|----|----|
| SL-001 | `journal/raw/_inbox/foundation-audit-p2/evidence/U8-memory-drift-specimen.md` | Verbatim specimen of the stale `journal/memory/v2-learning-foundations-roadmap.md` text (pre-correction), captured before the Q5 memory-drift fix, with line anchors and the contradicting `ROADMAP.md` / `PROGRESS.md` locations. This IS the U8 finding. | Task 0 Step 5 (Q5 memory drift) — for U8's audit pass |

<!-- Add further rows below as agents surface leads. -->
