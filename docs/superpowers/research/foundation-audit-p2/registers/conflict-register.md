# Conflict register

Agents return rows. IDs are assigned by the orchestrator. An agent that predicts an ID has made an
error.

<!-- Conflicts are RECORDED, not resolved, in Phase 2 — resolution is Phase 3. Agents propose a
     row (conflict description, the two sources, the units it touches); the orchestrator assigns
     the CFL-NNN id and appends the row. An agent that writes its own predicted id here has made
     an error (G6) — Phase 1 produced duplicate ids from concurrent agent-assigned appends, and a
     near-miss where a predicted id was relayed unchecked and would have written one card's
     content into another's row. -->

| Conflict ID | Sources in conflict | Description | Units affected | Status |
|----|----|----|----|----|
| CFL-001 | baseline `SCI-005` vs `how-to-teach.md` vs `C3-deliberate-practice.md` | `how-to-teach.md:105` states "Immediate correction is where misconceptions actually get fixed"; baseline `SCI-005` (U4 `:79`) found "delayed feedback was slightly stronger on average". A **third, definitional** position was surfaced by the U4 audit at `docs/superpowers/research/foundation-audit-p1/C3-deliberate-practice.md:68` (Ericsson et al.'s immediate-feedback requirement is a *definitional* criterion of deliberate practice, not a timing experiment) — so this is a three-way tension, not a two-way one. Anchors confirmed by the U4 examiner (handle a7c1a39aa8c11352f). | U4 (confirmed); U1 pending | OPEN — logged, not resolved (Phase 3) |

| CFL-002 | `docs/specs/product-vision.md:23` vs `docs/superpowers/specs/2026-07-16-adaptive-learning-product-activity-research.md:425-428` | Session length. The vision doc's "short learning loops, like Duolingo" names **one pole** of a Duolingo-vs-Brilliant session-length conflict the baseline already logs at `:425-428`. Proposed by the A-U2 examiner (a6ac18b4dfbd4fca3) as `CONF-U2-A`; anchors independently **confirmed verbatim** by the V-U2 verifier (aa60230c202c0282d). | U2 | OPEN — logged, not resolved (Phase 3) |
| CFL-003 | `docs/superpowers/research/foundation-audit-p1/dossiers/C4-chesscom-khan.md:433-439` vs `docs/specs/product-vision.md:58` | Mastery-progression external validation. Proposed as `CONF-U2-B`. Both agents agree this is a **standing caution on a model yet to be chosen, NOT a contradiction** of the audited line: `product-vision.md:58` names a loop stage and asserts no efficacy. Recorded so a later phase inherits the caution, not a false conflict. | U2 | OPEN — logged, not resolved (Phase 3) |
| CFL-004 | `how-to-teach.md:105` vs `SCI-005` (p.466, version of record) vs `C3-deliberate-practice.md:68-69` vs `docs/specs/learning-mastery-and-scoring.md:133-134` | Feedback timing, **four positions**: the import's unhedged "immediate correction"; SCI-005's "delayed slightly more effective" (and "clarity and consistency … more essential than the actual timing"); C3's *definitional* immediacy requirement (Ericsson 1993 p.367, tier Q4 at `C3:60-61`); and U1's own mode-scoped variant. Supersedes CFL-001's two-way form at U4. **All four anchors independently re-verified verbatim by V-U1** (abb68d3d4c1a434c2), which reached SCI-005's version of record via the RUG repository after the Springer DOI redirected to an auth wall. **Addendum from that re-verification:** SCI-005's own meta-regression (VoR p.470, Table 5) found feedback timing **did not contribute significantly** — which bears on the whole conflict and is recorded here so a later phase inherits it. | U1, U4 | OPEN — logged, not resolved (Phase 3) |
| CFL-005 | `docs/specs/learning-mastery-and-scoring.md:178`, `:184-185` vs `docs/superpowers/specs/2026-07-16-adaptive-learning-product-activity-research.md:251` (ALR-022) | Speed as evidence. U1 lists "response time" and "table pace" as scoring factors; ALR-022 requires evidence "without equating speed with ability". **NOT LIVE — confirmed by V-U1:** U1's factors sit under an explicit "Factors later:" deferral header, so nothing currently equates speed with ability. Recorded so the tension is inherited if the deferral is ever lifted. | U1 | OPEN (dormant) — logged, not resolved (Phase 3) |
| CFL-006 | AL-R2 `:136` vs AL-R2 `:83` vs `docs/superpowers/specs/2026-07-16-adaptive-learning-product-activity-research.md:392` | Learner-identity wording. AL-R2 says "at most pseudonymous" at `:136` but "one **anonymous** local learner" at `:83`; the baseline repeats the bare "anonymous" wording at `:392`. A privacy over-claim in the weaker wording — the same drift class that decisions row 25 was written to correct, reappearing in the documents row 25 cites. Proposed by the A-U5 examiner (aa8b8526251526e39); row 25's own claim is unaffected. Awaiting V-U5 confirmation. | U5 | OPEN — logged, not resolved (Phase 3) |

| CFL-007 | `docs/superpowers/specs/2026-07-10-v2-learning-foundations-roadmap-design.md:56-57` vs `docs/superpowers/research/foundation-audit-p1/dossiers/C2-its-actr-procedural.md:190-198` | **Curriculum sequencing: category-blocked-then-mixed vs interleaved practice.** The roadmap expands mastery "hard totals, hard doubles, soft totals, pairs/splits, **then** mixed review"; dossier finding F11 (Rohrer, Dedrick & Burgess 2014, Q2 randomised counterbalanced classroom RCT, grade-7 maths, n=140) reports interleaved practice beating blocked **72% vs 38%, d=1.05** on a directly analogous "choose the right strategy for this kind of problem" task — with domain transfer to adult blackjack **untested**, per the dossier's own caveat. Carried in-unit as verdict `K-U7-009` (`Revise`, not `Replace`/`Remove`, precisely because transfer is untested). **Both sides re-verified verbatim twice** — by the U7 examiner and independently by the LV-U7 landing confirmer (aa67160a022372d66). | U7 | OPEN — logged, not resolved (Phase 3) |

<!-- CFL-007 is the single most product-consequential row in this register: it is the one place where
     the project's own held evidence points against a sequencing decision already written into the
     roadmap. It is recorded and NOT resolved, per the phase boundary — resolving it is a later
     phase's job, and designing the sequence is later still. Flagged here because a reader scanning
     for "what did this audit actually find that changes the product" should land on this row. -->

<!-- Add further rows below as agents surface conflicts. Do not resolve here; record only. -->
