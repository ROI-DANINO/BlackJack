# Landing confirmation — `C-U5-003`

> Independent landing confirmer. No prior involvement in U5: did not raise `C-U5-003`
> (`verification/V-U5-citation-ruling.md` did), did not land it (`landing/L-U5.md` did). No `Bash`,
> no `Edit`. This file is the only write of this pass.
> Run: foundation-audit-p2, 2026-07-22.

## Verdict

| C-U5-003 | LANDED | `audit/U5-audit.md:166-177` (provenance paragraph + `<!-- LANDED C-U5-003 … -->` marker) |

## What was checked

**1. Annotation present and says what the ruling asked.**
`audit/U5-audit.md:166-176` reads (verbatim, in full):

> **Provenance note on the `CIT-U5-B` row of the internal citation table above (`:136`).** Every
> state value in that table, including this `VERIFIED` cell, is **examiner-assigned, not
> verifier-assigned** — it is the audit record marking its own citation, which is precisely what
> the separate-verifier contract forbids. For `CIT-U5-B` specifically, a fresh verifier's ruling
> (`verification/V-U5-citation-ruling.md`) finds this cell's `VERIFIED` **coincides with the
> correct outcome but is not corroboration of it**: the cell "was wrong at the time it was written,
> since it asserted verification for a `Revise` the sources refute," and is "right by accident."
> The authoritative state for `CIT-U5-B` is the verifier's ruling — `VERIFIED`, for the surviving
> `Preserve` at `K-U5-002` — as recorded in `registers/citation-state-register.md` and
> `verification/V-U5-citation-ruling.md`. The `VERIFIED` value in the table cell above is left
> unchanged; only its provenance is annotated here.

This matches the ruling's ask on both required elements: (a) the table's state values are
examiner-assigned, not verifier-assigned; (b) the authoritative state lives in
`registers/citation-state-register.md` and the verifier's ruling. I independently re-opened
`verification/V-U5-citation-ruling.md:182` and confirm the quoted fragments ("right by accident",
"wrong at the time it was written… a `Revise` the sources refute") reproduce that file's own text
verbatim — not a paraphrase drifted from it.

**2. `VERIFIED` value itself unchanged.**
`audit/U5-audit.md:136` (the internal citation table, re-read by me): `| CIT-U5-B
(web/src/learn/content/blackjack-basics.ts; docs/superpowers/specs/2026-07-15-strategy-table-fundamentals-lesson1-design.md)
| K-U5-002 | U5 | VERIFIED |` — still `VERIFIED`, unedited. The new paragraph is appended after the
table and after the existing `C-U5-001` marker; it does not touch the cell. Confirmed: **not
altered**. This is an annotation, not a reassignment — the editor did not do a verifier's job.

**3. Verdict-row survival.**
Ran the exact regex `^\| *K-U5-[0-9]{3} *\| *(Preserve|Relabel|Revise|Replace|Remove) *\|` against
`audit/U5-audit.md` myself (`Grep`, not eyeballed): **17 matches**, at lines 91, 92, 94, 95, 96, 97,
98, 99, 100, 101, 102, 103, 104, 105, 106, 107, 108 — `K-U5-001` through `K-U5-017`, one row each,
none missing, none duplicated. Read each row's verdict cell directly: `Relabel` at line 99
(`K-U5-008`) only; all other 16 rows read `Preserve`; zero `Revise`. **Count: 17. Distribution: 16
Preserve, 1 Relabel, 0 Revise — matches exactly.**

**4. Marker placement.**
`audit/U5-audit.md:177`: `<!-- LANDED C-U5-003 (V-U5-citation-ruling, editorial): marks the
CIT-U5-B row of the internal citation table (:136) as examiner-assigned, not verifier-assigned; the
coincident VERIFIED value is unchanged; authoritative state is the verifier's ruling in
registers/citation-state-register.md and verification/V-U5-citation-ruling.md. -->` — on its own
line, directly below the provenance paragraph, above the `## Every row that cites a document by
path…` heading (`:179`). Not inside any table: the citation-state table ends at `:150`; the
paragraph and marker sit at `:166-177`, outside it. Confirmed not inside a cell.

**5. Retrieval reproduced.**
I independently opened `verification/V-U5-citation-ruling.md` in full and confirmed its line-182
table cell for `audit/U5-audit.md:136` reads exactly as `landing/L-U5.md`'s C-U5-003 row quotes it
("Coincides with my ruling, but its provenance is invalid… examiner-assigned — a self-verified
state, which is the thing this program forbids… Do not treat this cell as corroboration of my
ruling; it is right by accident") and its Corrections-raised table row at `:192` names
`audit/U5-audit.md:136` and instructs the provenance mark.

I independently opened `verification/V-U5.md` and confirmed, myself, its citation-states table at
`:171`: `| CIT-U5-B | Revise | U5 | UNVERIFIED |`, and its prose at `:188-192` gives the
use-specific `UNVERIFIED` reasoning ("the sources were obtained and their quoted strings are
verbatim and line-exact, but the source contradicts the use made of it (half b) and the second
source carries an unverified status label doing load-bearing work (half a)…") — matching
`landing/L-U5.md`'s claim word for word. **Retrieval reproduces.**

**6. Scope.**
`landing/L-U5.md`'s C-U5-003 row states the editor did not open or edit
`registers/citation-state-register.md` ("orchestrator-owned; out of write scope"). I opened that
register myself: its `CIT-U5-B` row (`:87`, `| CIT-U5-B | Preserve | U5 | VERIFIED |`) and its long
comment block (`:89-169`) attribute that change to the **orchestrator**, writing from the
verifier's returned row — a separate action from this landing, with its own provenance trail
(`ledger #42`, "Source: `verification/V-U5-citation-ruling.md`, by a fresh audit-verifier…
Written here by me from that verifier's returned row"). Nothing in the register references
`landing/L-U5.md` or a landing editor. Consistent with the claimed scope: the `C-U5-003` landing
touched only `audit/U5-audit.md` and `landing/L-U5.md`; the register's `CIT-U5-B` update is a
distinct, orchestrator-attributed act, not this correction's landing. I found no edit inside
`verification/V-U5.md` or `verification/V-U5-citation-ruling.md` inconsistent with their own
internal "only file written this pass" declarations, and no content in either suggesting
post-hoc tampering.

## Scope discipline observed

I did not re-litigate the ruling's remedy choice (annotation vs. reassignment) — that was settled
upstream. I raise no new corrections. The one process fact worth flagging for the orchestrator
(not a correction, an observation already fully self-disclosed in the artifacts themselves): the
register's own comment block documents that an *earlier* orchestrator edit to `CIT-U5-B` (changing
cell 2's prose without a verifier ruling) was independently judged "EVADED, not earned" by the
`V-U5-citation-ruling.md` pass, and was itself superseded before this `C-U5-003` landing occurred.
That episode is upstream of and unrelated to `C-U5-003`'s landing quality, which is clean on all
six checks above.

## File re-read confirmation

This file was written to
`journal/raw/_inbox/foundation-audit-p2/verification/LV-U5-003.md` and then **re-read from disk
with `Read` before returning**, confirming the content above matches what was written. It is the
only file this pass wrote.
