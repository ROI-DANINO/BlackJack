# Verification — Unit U1

> Verifier instance, independent of the `audit-examiner` that wrote `audit/U1-audit.md`.
> Run: foundation-audit-p2. Written 2026-07-21.
> Scope: `audit/U1-audit.md` against `docs/specs/learning-mastery-and-scoring.md`, the P1 archive at
> `docs/superpowers/research/foundation-audit-p1/dossiers/`, the baseline
> `docs/superpowers/specs/2026-07-16-adaptive-learning-product-activity-research.md`, and the upstream
> import `docs/imports/v2-research-2026-07-11/research/v2-research-02-curriculum-and-pedagogy.md`.
> Every anchor cited below was reopened in this pass; nothing is accepted on the record's word.

SUFFICIENCY: INSUFFICIENT

The record's central negative — that **no** Roediger & Karpicke descendant claim survives into U1 —
is not sustainable against U1's own text or against the baseline the record itself quotes, and the
U1 claim family that carries that descendant (progressive withdrawal of support: `U1:22-24`, `:35`,
`:87-95`) was never assessed as a claim. The gap is bounded and **needs no collection**: the
supporting material is already inside sources the record holds and cites. Two editorial corrections
close it.

## Mandatory calibration — Remove/Replace defect-real answers

**The audit record carries zero `Remove` and zero `Replace` verdicts.** No `DEFECT-REAL` row is
required, and this section is retained deliberately to record that fact rather than deleted.

**Is zero the honest number here?** Yes. I re-read all 18 assessed claims against the evidence in
hand and found nothing in U1 that the project's held evidence contradicts. Every candidate for a
`Remove` I could construct dissolves on inspection:

- The accuracy-before-speed gate (`K-U1-004`) is *unsupported*, not *contradicted* — C6's Phillips &
  Amrhein entry is too weak to carry it in either direction. Absence of support is a `Relabel`, not a
  `Remove`; the record made that call correctly.
- The immediate-for-acquisition split (`K-U1-014`) sits in genuine tension with SCI-005 but is not
  contradicted by it: SCI-005's finding concerns *within-condition mixing*, U1 prescribes a
  *mode-scoped* split. The record's "tension, not contradiction" reading is right, and is
  independently strengthened by material the record did not use — SCI-005's own meta-regression
  (version of record, p. 470 and Table 5) found feedback **timing** did not contribute significantly
  to the effect on learning performance at all.
- The chess-style internal rating (`K-U1-016`) has an unstated population dependency (`C1:244`), but
  a dependency the product cannot yet satisfy is an Assumption, not a falsified claim.

The pessimism direction I was warned about does not appear in this record either: I found **no
manufactured defect**. All five `Relabel` verdicts are earned (see below). The one place the record
runs *optimistic* is the Roediger & Karpicke negative, which is the correction I raise.

## Corrections raised

| ID | Correction | Remedy |
|----|----|----|
| C-U1-001 | The record's strongest negative is false as stated. At `U1-audit.md:68-70`: "**Roediger & Karpicke 2006** and **Bjork & Bjork** — no explicit descendant claim survives into U1 at all. There is nothing to be baseless: U1 states no testing-effect claim and no desirable-difficulties claim." The retrieval-practice half does not hold. U1 carries an explicit unaided-recall / support-withdrawal claim family, and it is a direct textual descendant of the import: import `:14` reads "**Decision recall** — choose the recommended action with and without support."; U1 `:35` reads "4. Decision recall: choose the recommendation with progressively less support." U1 also states `:23` "Play correctly without the table and without time pressure." and `:92` "4. table-hidden decision;". The project's own baseline attaches **retrieval** evidence to exactly these: `…-research.md:39` records `SCI-001` as "Repeated retrieval and delayed recall for learned vocabulary"; `:74` "**INFERENCE:** require learners to produce independent first responses and revisit retrieval after feedback; repeated viewing or answer-assisted retries are not equivalent mastery evidence."; `:87-88` "**Targeted repetition:** supported in principle by retrieval and spacing evidence (`SCI-001`, `SCI-002`)…" — a sentence the record quotes at `K-U1-010` while attributing the strength to Cepeda alone and dropping the retrieval half; `:253` (ALR-022) "**INFERENCE:** retrieval evidence `SCI-001`"; `:141` (ALR-004) "assisted success can support learning but cannot satisfy an unassisted mastery or skip-test requirement." **Attribution hazard the fix must not walk into:** the import cites Roediger & Karpicke (2006), test-enhanced learning; the baseline's `SCI-001` is a *different* paper — Karpicke, J.D. & Roediger, H.L. III (2008), "The Critical Importance of Retrieval for Learning," *Science* 319(5865), 966–968, DOI 10.1126/science.1152408, opened and read in this pass (abstract, verbatim: "Repeated studying after learning had no effect on delayed recall, but repeated testing produced a large positive effect."). Reversed author order, different year, different journal; they are not interchangeable. The Bjork & Bjork half of the sentence is **sound and should stand** — U1 nowhere claims that harder practice conditions depress immediate performance while improving retention. | editorial — the correcting material is inside sources already held (baseline `:39`, `:74`, `:87-88`, `:141`, `:253`; import `:14`; U1 itself). No collection. |
| C-U1-002 | A live U1 claim family was never assessed. `K-U1-003` assesses the *ordering* of `U1:30-38`; `K-U1-004` assesses only rung 7 of the exercise ladder; the ladder's substantive content — progressive withdrawal of support — is assessed nowhere, and the non-material notes touch it only as a rung-*count* drift (`U1-audit.md:206-208`). The unassessed text is `U1:22-24` ("1. Play correctly with the table open. 2. Play correctly without the table and without time pressure. 3. Play correctly without the table at realistic table pace."), `U1:35`, and `U1:87-94` ("Reuse concepts across progressively harder formats:" … "1. recognition; 2. guided classification; 3. table-open decision; 4. table-hidden decision; 5. full evolving hand; 6. interleaved review;"). This is the claim family with the *most* basis in hand, and leaving it out is what let the false negative in C-U1-001 stand: baseline `:59` "**INFERENCE:** support a guided first encounter and fade assistance before evidence is counted as independent mastery proof."; `:77` "**INFERENCE:** begin with a canonical worked state or nearly complete assembly, then fade blocks and hints; do not force the same scaffold on demonstrated experts." (`SCI-004`, `SCI-009`); `:387` rejects "Forced same-item retries until a correct answer appears" on `SCI-001`, `SCI-002` because "It can erase first-response evidence and substitute short-term repetition for later retrieval." | editorial — every basis named is in a source the record already consulted. No collection. |
| C-U1-003 | Quotation fidelity at `K-U1-016` (`U1-audit.md:102`). The record renders `C4:121` as: "structurally different from every model C1 examined… no rating, no confidence interval, no item-difficulty parameter, and no probability estimate appears anywhere in this definition." The dossier's actual text at `C4:121`, verbatim and in source order, is: "These are fixed accuracy-percentage thresholds and pass/fail checks on mixed-skill assessments — no rating, no confidence interval, no item-difficulty parameter, and no probability estimate appears anywhere in this definition. This is a structurally different mechanism from every model C1 examined (BKT, PFA, DKT, IRT, education-Elo)." The record's rendering **reverses the order of the two clauses across an unmarked ellipsis** and compresses "This is a structurally different **mechanism** from" to "structurally different from". The substance and the verdict are unaffected — this is why it is the smallest correction in this record — but it is a quotation presented as verbatim that is not, in a record that declares at `:12-13` "No other alteration is made; no ellipsis is used anywhere in this record." | editorial — source in hand (`dossiers/C4-chesscom-khan.md:121`). |

## Independent finding — do Roediger & Karpicke / Bjork & Bjork descendants survive into U1?

I searched U1 end to end for the retrieval-practice and desirable-difficulties constructs rather
than accepting the record's negative.

- **Roediger & Karpicke / retrieval practice: YES, a descendant survives.** Detail and verbatim
  anchors in `C-U1-001` and `C-U1-002`. The clinching item is textual lineage, not interpretation:
  the import's rung "**Decision recall** — choose the recommended action with and without support."
  (`…curriculum-and-pedagogy.md:14`) survives verbatim-in-substance as U1 `:35`, and the import's
  ladder rung 5, "production before buttons appear" (`…curriculum-and-pedagogy.md:62`) — free
  production before recognition options appear, i.e. the purest retrieval rung in the document — was
  **dropped** in the fold, an omission already flagged as an unanswered question at `PROGRESS.md:97-99`
  ("Was dropping the 'production' rung — the learner names the play before seeing options — from the
  exercise ladder in `learning-mastery-and-scoring.md:85-95` intentional?"). A live claim whose
  citation was stripped and whose basis was then never checked is exactly the defect this unit
  exists to detect, and the record recorded its absence instead.
- **Bjork & Bjork / desirable difficulties: NO surviving claim; the record is right.** U1 orders
  formats from easier to harder and withdraws support, but nowhere asserts the desirable-difficulty
  claim proper (that a manipulation which *depresses* performance during acquisition *improves*
  long-term retention). This half of the record's negative is sound and I explicitly decline to
  disturb it.
- **Cepeda 2006 → `U1:110-115`:** confirmed. `C5:41-63` is F1 Cepeda 2006, tier "Q1 (large-scale
  quantitative meta-analysis)" at `C5:48`, quote "Only 12 of 271 comparisons of massed and spaced
  performance showed no effect or a negative effect from spacing." verbatim at `C5:56-58`, with the
  verbal-memory scope limit at `C5:61-63`. Baseline `:87-88` confirmed. U1's own hedge at `:114-115`
  confirmed. `C5:84-106` (Donovan & Radosevich, "complex tasks d = 0.11 and d = 0.07 in the two most
  complex clusters") confirmed at `C5:98`.
- **Kornell & Bjork 2008 → `C5:165-193`:** confirmed, F6, and the record is right that U1's
  interleaving claim is better carried by `C5:108-134` (Taylor & Rohrer) and `C2:190-198` (Rohrer et
  al. 2014). The record did not use `C5:188-193`, which records that Verkoeijen & Bouwmeester (2014)
  replicated Experiment 1 but that "the findings from Kornell and Bjork's (2008) second experiment
  have never been replicated" — not needed for any verdict here, noted for completeness.
- **Ericsson 1993 → `U1:8-12`:** confirmed exactly as the record states. `C3:445-450` quoted
  verbatim, including "asserted or conceded by **both** camps" at `C3:440-441`; the non-surviving
  magnitude claim is at `C3:451-455`; F6's non-replication headline is at `C3:263`. I re-read `U1`
  end to end: it makes **no** hours-accumulation, magnitude, or 10,000-hour claim. The record's
  "provenance defect, not a substance defect" reading is correct.

## Spot-checks on the `Preserve` rows

I re-opened the basis for every `Preserve` row. Anchors, tiers, and figures all check out.

- **`K-U1-006` (`U1:58-66`) — the highest-stakes row: SOUND, both halves.** The "46% vs. 10%" figure
  is verbatim at `C5:121-124`: "blocked practice and interleaved practice produced about the same
  number of fabrication errors (15% vs. 13%, respectively), but interleaving of practice dramatically
  reduced the frequency of discrimination errors (46% vs. 10%)." Tier "Q2 (controlled experiment)" is
  at `C5:115`, attribution Taylor & Rohrer (2010) at `C5:116-117`. The transfer note is verbatim at
  `C5:132-134`, and the record correctly carried the school-geometry caveat rather than dropping it.
  The downstream consumption is real and in requirement voice:
  `2026-07-17-progressstore-cycle1-design.md:33-35` reads "Skill granularity (`'hand-total'`) is
  coarser than the per-cell keying mastery needs — `learning-mastery-and-scoring.md:56-66` requires
  classification and row/column navigation to be measurable separately from choosing the action."
  Direct fetch of the Taylor & Rohrer primary PDF failed on TLS
  (`SSL routines:OPENSSL_internal:UNSUPPORTED_PROTOCOL`); a secondary retrieval confirmed the 46/10
  figures exist in the paper. The dossier-level verification the dispatch asked for is complete;
  primary-level re-verification of that figure was not achieved in this pass and is not claimed.
- `K-U1-001`: `U1:3` verbatim; `progressstore…:34` confirmed as an active-spec requirement use. The
  escape clause reading is right.
- `K-U1-002`: `C3:445-450` and `C3:68-71` both verbatim, tier Q4 at `C3:60-61`.
- `K-U1-005`: baseline `:91-92` verbatim; `C5:120-126` and `C2:197` confirmed.
- `K-U1-008`: baseline `:95-97` and ALR-016 `:215` verbatim; import `:7` confirmed to read "**Verified
  design principle:**". The record's "drift in the honest direction" observation is correct and is
  the kind of finding an under-motivated pass omits.
- `K-U1-009`: ALR-032 `:313` and ALR-004 `:141` verbatim.
- `K-U1-010`: baseline `:87-88` and `C5:54-60` verbatim — **but partial**; see `C-U1-001`.
- `K-U1-011`: `C2:197` verbatim including "72 % vs. 38 %, d = 1.05"; tier at `C2:194`; caveat at
  `C2:198` ("Domain is grade-7 math… that transfer is untested") confirmed.
- `K-U1-012`: baseline `:93-94` and the rejected-option row at `:389` verbatim.
- `K-U1-015`: baseline `:89-90` verbatim; the "Example ladder" wording at `U1:141` supports the
  Preserve-not-Relabel call.
- `K-U1-018`: baseline `:334` and `:407` verbatim.

## Ruling on the five `Relabel` verdicts — all five earned, none manufactured

- **`K-U1-003`** (seven-stage progression → Assumption). `C4:113-121` is Khan's five-level ladder,
  tier "Q5 (official product documentation)" at `C4:117`; baseline `:98-100` verbatim, including
  "strict prerequisite selection and skip-test coverage remain deterministic project policy requiring
  proof-specific validation." Existence-proof-not-validation is the right reading. **Earned.**
- **`K-U1-004`** (accuracy-before-speed → Assumption). The dispatch asked me to test whether the
  record overstated C6's weakness. **It did not.** Every element checks out verbatim: N=12 at
  `C6:230-231` ("Twelve subjects played 20 hands under each of the experimental conditions."); the
  double hedge at `C6:241-244` ("The results **suggested** that deviations from optimal play can **in
  part** be understood…"); and at `C6:250-257` the C6-EC material caveat, verbatim: "The computer
  simulation of Blackjack used **simplified rules (no splitting, doubling down, insurance, etc.)**,
  and **the probability of winning was controlled by the computer.**" … "**V6 judges this limitation
  more severe than the N=12 the dossier did disclose.**" If anything the record *understated* by not
  quoting C6's closing instruction that "the construct-validity limit above should govern how this
  finding is used" (`C6:256-257`). **Earned.**
- **`K-U1-014`** (immediate-for-acquisition → Assumption). SCI-005 independently re-opened (below);
  both quoted sentences verbatim. **Earned.**
- **`K-U1-016`** (chess-style rating → Assumption). `C4:90` verbatim ("The new Puzzle ratings use the
  same Glicko-style rating logic as your chess games (blitz, rapid, etc.)"), tier Q5 at `C4:87`;
  `C4:101` verbatim on crowd calibration ("it is population/crowd calibration of item difficulty, not
  a population-free seeding method"), with the underlying help-page quote at `C4:100` ("its rating is
  determined by who is able to solve it"); `C1:244` verbatim on "at least 100 students" to get good
  item-difficulty estimates. The unstated-dependency finding is real. **Earned** (subject to
  `C-U1-003` on the `C4:121` rendering).
- **`K-U1-017`** (error severity → Product judgement + COVERAGE GAP). ALR-022 verbatim at
  `…-research.md:251`, including "without equating speed with ability". **Earned.**

## Citation state (verifier-assigned)

| SCI-005 | Preserve | U1 | VERIFIED |
| SCI-005 | Relabel | U1 | VERIFIED |

**Independently reproduced, not accepted.** The Springer DOI landing page redirects to
`idp.springer.com` authorization and could not be read directly in this pass; I obtained the
**version of record** PDF from the University of Groningen repository
(`https://pure.rug.nl/ws/portalfiles/portal/1198610490/s10984-024-09501-4.pdf`, cover page: "Document
Version: Publisher's PDF, also known as Version of record") and read it. Bibliographic identity
confirmed: Brummer, L., de Boer, H., Mouw, J., & Strijbos, J.W. (2024), "A meta-analysis of the
effects of context, content, and task factors of digitally delivered instructional feedback on
learning performance," *Learning Environments Research*, 27, 453–476, DOI 10.1007/s10984-024-09501-4.

Both sentences the record quotes are **verbatim**, in the Discussion → "Context factors" subsection,
p. 466:

> "With regard to feedback timing, both immediate and delayed feedback had significant and strong
> effects on improving learning performance, with delayed feedback being slightly more effective than
> immediate feedback. A combination of feedback timing approaches was ineffective. These findings
> indicate that clarity and consistency—as to whether participants receive immediate or delayed
> feedback—is more essential than the actual timing of the feedback."

Independence and funding checked on the disclosure page (p. 471): "**Funding** No funding was
received for conducting this study." and "**Conflict of interest** The authors have no relevant
financial or non-financial interests to disclose." Open Access, CC-BY 4.0. Not vendor-authored; a
legitimate independent corroboration. Tier: the design is a meta-analysis of 116 interventions from
46 articles, which warrants Q1 on design grounds. No corrigendum or erratum is appended to the
version of record, and the article page carries no retraction notice.

**Citations verified in this pass but attached to no current verdict** (returned so the editor
landing `C-U1-001` need not re-retrieve, and so no `Preserve` is ever rested on an unverified
source): `SCI-001` — Karpicke, J.D. & Roediger, H.L. III (2008), *Science* 319(5865), 966–968,
DOI 10.1126/science.1152408. Primary opened and read (title page + abstract, MIT-hosted copy of the
Science page). Abstract verbatim in part: "Repeated studying after learning had no effect on delayed
recall, but repeated testing produced a large positive effect." Population is university students
learning foreign-language vocabulary — the same scope limit the baseline already records at `:39`
("does not determine product scheduling or mastery thresholds"). No citation-state row is claimed for
it here because it supports no verdict in the record as written; the orchestrator should attach a row
only once the landed verdict exists.

## Ruling on the bounded gap spec — DECLINED

The record proposes, on a verifier's call, one focused pass (≤4 citations) on: *does any published
source grade the instructional cost of situation→action rule errors by severity tier?* I **decline to
commission it** and rule it a correctly-recorded, **settled** `COVERAGE GAP`. Reasoning, so it is not
re-opened by a later pass:

1. The pass cannot move a verdict. `K-U1-017`'s bucket is **Product judgement** — a values decision
   about what the product should penalise. A found source would not convert it to Evidence-backed; a
   null result would not change it either. A collection pass whose two possible outcomes leave the
   verdict identical is not worth a collector.
2. The severity ordering it would try to ground is not an instructional-literature question in the
   first place. An EV-cost ordering over blackjack misplays is computable from the project's own
   strategy oracle, which is a product-side question outside this program's collection scope.
3. The record already looked. "We looked hard and it isn't there" is the result, and I am recording
   it as one.

## Ruling on the conflicts — both correctly recorded, anchors confirmed

**Feedback-timing, four positions.** All four anchors re-opened and confirmed verbatim: position 1 at
`docs/imports/v2-research-2026-07-11/course-bundle/how-to-teach.md:105` ("**Immediate and
corrective.** Right after each decision, not batched. Immediate correction is where misconceptions
actually get fixed."); position 2 in SCI-005 p. 466 (above) and carried in the baseline at `:43` ("do
not support one universal timing rule") and `:434-438`; position 3 at `C3:68-69`, verbatim, tiered Q4
at `C3:60-61` — the record's reading that this is a *definition of the construct* and not a timing
experiment is correct and is the sharpest thing in the conflict block; position 4 at `U1:133-134`.
The record's observation that U1 **declined** to inherit `how-to-teach.md:105` is correct and is drift
away from over-strength. Recorded, not resolved — correct handling. One item the record could have
carried and did not: SCI-005's own meta-regression (p. 470, Table 5) found that feedback timing "did
not contribute to the effect on learning performance", which sharpens position 2 further. Not a
correction; noted below.

**Second, minor — "response time"/"table pace" vs ALR-022.** Anchors confirmed: `U1:178` reads
"Factors later:" and `U1:184-185` reads "- response time;" "- table pace;"; ALR-022 at
`…-research.md:251` reads "…and error classification without equating speed with ability." The
record's "not a live contradiction, because U1 defers these" characterisation is **accurate** — the
factors sit under an explicit deferral header. Correctly logged so it is not met as a surprise later.

## Phase-boundary rules — line references only

1. **States what the project should now do:** no breach found. The two closest lines, examined and
   cleared: `U1-audit.md:98` ("it is the calibration bar the other mastery claims should have met") —
   a retrospective judgement about the audited document, not a directive to the project; and
   `U1-audit.md:171-177` (the gap spec) — explicitly conditioned on a verifier's call, which this
   dispatch authorised, and ruled on above.
2. **Authored replacement content:** none. Zero `Replace` verdicts; confirmed by reading every row.
3. **Decision-vocabulary collision:** no breach. `Confirm` and `Reconsider` appear nowhere in the
   record (the only near-match is "re-confirmed" at `:4`, describing the examiner's own reading).
   `Revise` (`:9`, `:89`, `:100`, `:106`, `:168`) and `Remove` (`:9`, `:89`, `:100`, `:106`, `:119`)
   are used exclusively in this phase's verdict sense.

## Non-material notes (mechanical / cosmetic — not gated)

- `U1-audit.md:12-13` declares "no ellipsis is used anywhere in this record"; ellipses appear at
  `:97` (`K-U1-011`), `:102` (`K-U1-016`) and `:133` (conflicts block). The `:97` one faithfully
  reproduces an ellipsis present in the dossier's own text at `C2:197`, so only the convention
  statement is wrong there; `:102` is material and is raised as `C-U1-003`; `:133` elides without
  reordering and does not change meaning.
- `U1-audit.md:49-52` adds bold emphasis ("**requires**", "**forbids**") to quoted text from
  `2026-07-17-progressstore-cycle1-design.md:34` and `:130-131`; neither source carries emphasis
  there. Emphasis added to a quotation should be marked as added.
- `U1-audit.md:52` anchors the "forbids destroying evidence" clause at `:130`; the clause spans
  `:130-131`.
- The record's `C6` anchor style is inconsistent with its own stated convention: `K-U1-004` cites
  `C6:250-257` for a quote that ends at `:253` and a V6 judgement at `:255-256`. Reads correctly; the
  range is simply wider than the quoted text.
- `U1-audit.md:106` reports "18 claims assessed" without stating coverage. The 18 rows cover 6 of
  U1's 15 sections; the deliberate exclusions are enumerated at `:186-208`, but the ladder sections
  are excluded only in part (the substantive half is the subject of `C-U1-002`).
- SCI-005's meta-regression null on feedback timing (version of record p. 470, Table 5) is additional
  in-source support for `K-U1-014`'s "tension, not contradiction" framing. Optional; the verdict
  stands without it.

## Verifier's own limits, stated

- The Taylor & Rohrer (2010) primary PDF could not be opened (TLS protocol failure at
  `uweb.cas.usf.edu`); the "46% vs. 10%" figure is verified **at dossier level, verbatim**, plus a
  secondary confirmation that the figures appear in the paper. Primary-level verification of that
  figure is not claimed by this pass.
- The P1 dossiers were treated as evidence input, per dispatch. I did not re-litigate their approved
  gate; where I quote them I am verifying that the audit record read them correctly, not re-verifying
  their own citation states.
