# Verification — Unit U1

Assembled by the orchestrator from rows returned by two independent `audit-verifier` instances.
Neither collected this unit; the second is not a continuation of the first.

## Pass 1 — initial verification

**Citations:** all 10 opened sources re-opened independently. Every quotation checked matched
**character-for-character** — the collector's self-declared highest risk, text-extraction drift
across 8 of 10 sources, did not materialise. Zero quote defects.

**UNREACHABLE audit:** clean, enumerated positively. No pattern row cites U1-S11, U1-S12 or U1-S13;
two appear only in "not assessed" rejection rows explaining why nothing is claimed, and one only in
the source register.

**Upheld rather than overturned:** the collector invited reconsideration of its conservative
provenance call on U1-S5. This verifier re-opened the source and upheld the collector.

**Sufficiency: INSUFFICIENT** — for depth, not coverage. Two sources already on disk were read at a
fraction of their length, and the unread portions contain direct evidence on questions the collector
left open. One evidence family, medical education's key-features and script-concordance instruments,
was unengaged.

## Top-up collection

Added one qualifying pattern, three rejected-candidate rows with their failing tests named, and
three sources. Script concordance testing was rejected on clause (b) as predicted, with two of its
scoring ideas extracted as transferable. The lead inside a held source was checked and held on one
half only, enumerated positively in both directions.

## Pass 2 — re-verification, fresh instance

**Citations:** 30 new or changed citations checked. 26 `VERIFIED` clean, 3 `VERIFIED` with a locus
or strength defect, 1 `DROPPED`. No fabricated source, no fabricated locus, no wrong-author
attribution anywhere in the new material.

**Priority 1 — U1-7's qualification, the collector's own declared closest call: HOLDS.** Narrowed,
not deleted.

**Priority 2 — the null result: correctly reported.** Numbers, direction, sample and the source's
own caveats all check out.

**Priority 3 — cross-effect on U1-1 … U1-6: no overclaim found, and no relabelling recommended.**
The null compares free text against long-menu, itself a generate-then-confirm production format
rather than a multiple-choice one, so it does not show constructed-response converging on MCQ; the
same paper shows the production formats scored higher than the MCQs and took longer. None of the
older rows asserts empirical superiority over a dealt-hand format; each states what its designers
claim, labels itself `Product judgement`, and flags its own inferences.

**Recorded near-miss:** one extraction pass reported 70 references where the file states 66. The
verifier re-checked, found 66 correct, and recorded that it had nearly filed a finding that did not
exist — "the shape of the pessimism trap."

**Position on pass 1's corrections:** all eight still stand. C4 independently re-derived and
confirmed at source.

**Sufficiency: INSUFFICIENT** — the named gap is closed and closed cleanly; the unit fails on the
program's dominant defect class, three pieces of directly on-point material sitting unread inside
the two sources the top-up itself added. Remedy is `editorial` with zero new sources.

SUFFICIENCY: INSUFFICIENT
