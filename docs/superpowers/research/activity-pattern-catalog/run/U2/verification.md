# Verification — Unit U2

Assembled by the orchestrator from rows returned by two independent `audit-verifier` instances.
Neither collected this unit.

**Note on history, recorded because the record should be honest.** A first verifier instance for
this unit was terminated mid-pass by an account spend limit and its transcript proved
unrecoverable. Before dying it signalled that it had found something material but never said what.
That finding is lost. The pass below is a fresh full verification; the lost signal was passed to it
as *unknown*, explicitly not as confirmation that a defect existed.

## Pass 1 — verification (fresh full pass after the loss)

**Citations:** 17 of 19 sources `VERIFIED`, 2 `UNVERIFIABLE` — both already recorded `UNREACHABLE`
with their HTTP failures independently reproduced, and both correctly cited nowhere. 0 `DROPPED`;
no pattern is baseless.

**Hard gate:** PASS, enumerated positively by listing every Source ID appearing in the pattern and
rejection tables. Neither unreachable source appears in either.

**Confirmed good behaviour, checked rather than assumed:** the conservative provenance call on a
Duolingo-funded peer-reviewed study upheld, with the funding statement and author affiliations
verified verbatim; the handling of the corpus's unbacked recognition-versus-production lead judged
exemplary — reported unbacked, refused for downstream citation, counter-evidence stated with its
funder and the authors' own confound both disclosed; three absence claims confirmed first-hand; a
partial-render caveat judged the correct status rather than a fudge.

**Sufficiency: INSUFFICIENT** — the gap is stopping short, not fabrication. Evidence sat one hop
from sources already in hand: a sibling post in a blog series already cited, and a report in the
same directory as a source already fetched.

## Top-up collection

Added one qualifying pattern, one rejected-candidate row, and three sources, all opened first-hand.
Raised a correction **against its own dispatch**: a statistic the orchestrator's brief had relayed
as bearing on one product in fact concerns a different one. Declined to reproduce a quotation it
could not vouch for character-for-character. Reported one gap as narrowed rather than closed after
reading the source cover to cover.

## Pass 2 — re-verification, fresh instance

**Citations:** all three new sources, the new pattern and the new rejected row `VERIFIED`; every
quoted passage verbatim at the stated locus. Two previously-cited loci `DROPPED` as rendered — quote
repairs, not source removals.

**The correction against the dispatch is upheld**, verified first-hand at the source: the statistic
concerns Babbel learners, not Duolingo. The orchestrator's brief was wrong.

**A collector self-assessment overturned.** The collector reported one qualification as "now
sourced." This verifier disagrees: the disclosure is honest but lives in prose far from the cell,
while the table row the archive actually carries remains unhedged and cites the old source. Status
is narrowed, not closed.

**A prior finding downgraded.** One raised correction claimed a source title was recorded wrongly;
the audit had used the page's meta title, which is not an error. Resolved against the raised
finding.

**An under-claim identified.** One truncated quotation makes the audit *weaker* than its own source,
which described a failure mode as likely where the audit renders it as possible.

**A schema ruling supplied, and a hole in the orchestrator's response to it.** The two-token
provenance enum cannot express "third-party publisher that discloses compensation." The verifier
supplied a general rule and the orchestrator adopted a third token for this unit. The verifier then
noted that `independent` rows in the other two units were assigned under the two-token scheme and
were never checked for compensation — recorded here so a cross-unit reader does not over-read them.

**Declined rather than rubber-stamped:** ten pending corrections whose text the orchestrator had
summarised rather than supplied. The verifier refused to certify corrections it had not read, citing
the absence-as-proof rule. The text was supplied and positions returned in a follow-up: nine still
stand, one downgraded, two upgraded to material, exactly one duplicate identified.

**Sufficiency: INSUFFICIENT** — narrowly, by `editorial` route only, zero new sources. One directly
on-point passage inside a source read cover to cover did not reach the record, and a vendor
concession is quoted without the walk-back that immediately follows it.

**On the provenance limit:** once C1 lands this unit has no reachable source labelled `independent`.
The verifier judged the orchestrator's intent — state the limit plainly rather than chase more
sources — defensible, on the condition that C1 and C2 land in all their loci. Without them the
archive would ship one evidence-backed claim resting on an unquantified impression from a
compensated publisher, and the plain statement would itself be false.

SUFFICIENCY: INSUFFICIENT
