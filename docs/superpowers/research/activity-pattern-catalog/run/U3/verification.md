# Verification — Unit U3

Assembled by the orchestrator from rows returned by two independent `audit-verifier` instances.
Neither collected this unit; the second is not a continuation of the first.

## Pass 1 — initial verification

**Citations:** 7 `VERIFIED`, 3 `UNVERIFIABLE` (all three already recorded `UNREACHABLE`; the 403 on
Chi et al. was independently reproduced), 0 `DROPPED`.

**Hard gate:** PASS, enumerated positively. Table 1's Source ID column across all nine rows reads
U3-S2, U3-S2, U3-S1, U3-S7, U3-S7, U3-S4, U3-S5, U3-S6, U3-S3. None of S8/S9/S10 appears.

**The unit's "9 of 9 Evidence-backed" claim:** 8 of 9 survive. One row must drop to
`Product judgement` (C1).

**Every statistic re-checked** against Tables 2–5 and the cited pages for number, direction and
sample. All matched except the four raised as C1–C4.

**Confirmed good behaviour, checked rather than assumed:** the Kang et al. self-flag is accurately
recorded as *what one source says about another*; the Runestone conflict is real, disclosed, and
`independent` still correct for a peer-reviewed ACM systematic review; both counter-directional
results are reported accurately including the source's own hedge; the word-bank row survives the
qualification stress-test. This verifier also reversed two of its own preliminary suspicions after
reopening the sources, and recorded doing so.

**Sufficiency: INSUFFICIENT** — one bounded `collection` gap: construct-equivalence counter-evidence
for constructed-response versus stem-matched multiple choice, unengaged.

## Top-up collection

Closed the gap by register note rather than pattern row. Rodriguez (2003) proved unobtainable across
six enumerated retrieval routes and is recorded `UNREACHABLE`, cited by nothing. A later, larger,
open-access synthesis was opened in full and cross-checked against the publisher's JATS XML.

## Pass 2 — re-verification, fresh instance

**Citations:** `U3-S11` `VERIFIED` at all twelve loci, page numbers included — checked individually.
`U3-S12` `VERIFIED (metadata) / UNVERIFIABLE (content)`, correctly handled. Independence of
`U3-S11` confirmed directly at p.20: no competing interests, no funding, university authors, CC-BY.

**The "near unity" reversal: supported**, by three independent confirmations from the source, and
more strongly than the register note itself states — `U3-S11` p.3 carries Rodriguez's own headline,
an uncorrected pooled correlation of 0.67, which settles the disputed claim from a source that read
it. Scope limit recorded: this defeats near-unity as an *uncorrected* claim; it does not adjudicate
a disattenuated figure, and it does not refute that stem-equivalence matters at score level.

**The flagged internal inconsistency in `U3-S11`:** the collector's handling is honest and, on this
verifier's arithmetic, understated — the studies-versus-effect-sizes pairing is a real internal
contradiction in the published paper, not merely a units mismatch. The register note's conclusion is
robust to it either way.

**Cross-effect on U3-1 … U3-9:** no overclaim. Both exposed rows make score-level claims, and score
level is where the new source agrees with them. No row's qualification, evidence label, or
provenance changes.

**Position on pass 1's corrections:** C1, C2, C4 and the "One classroom" item confirmed at source.
**C3 disputed and reversed** — the raised value appears to read a different column than the one
cited; the dossier as written is correct. Remaining non-material items: no position taken, declined
rather than rubber-stamped.

**Sufficiency: INSUFFICIENT** — narrowly, and by `editorial` route only, with zero collection
required. Two pieces of directly on-point material sit unread inside `U3-S11`, a source this pass
read in full: p.3's summary of the entire prior meta-analytic literature on the unit's own question,
and Meta-Analyses C, a third of the paper, including an external-criterion result that bears on the
boundary and corroborates an existing row.

SUFFICIENCY: INSUFFICIENT
