# C2 — ITS / ACT-R / procedural learning · editorial correction pass

**Dispatch as:** `audit-editor` · model `opus` · Wave 1 (parallel with C1, C3–C5)

## Where this fits
C2 passed citation verification but was judged INSUFFICIENT — and its gap costs **zero new
citations**. The material is already inside sources the dossier cites. It was never extracted.

## Remedy type: editorial, NOT collection
Amendment 7 created this role for exactly this case. You have **no WebSearch** — you structurally
cannot collect. Use WebFetch only to re-read sources the dossier **already cites**. If the correction
genuinely cannot be made from sources in hand, stop and report a `Blocker`; escalating to collection
is the orchestrator's decision.

## Read first
1. `journal/raw/_inbox/foundation-audit-p1/C2-its-actr-procedural/dossier.md`
2. `journal/raw/_inbox/foundation-audit-p1/verification/V2-its-actr-procedural.md`
3. `journal/raw/_inbox/foundation-audit-p1/verification/V2b-its-actr-procedural-toppedup.md` — names the gap
4. `journal/raw/_inbox/foundation-audit-p1/_templates/evidence-quality-rubric.md`

## Extract and correctly incorporate
1. **The report-type moderator robust under both models** — peer-reviewed g=.28 vs nonjournal g=.02,
   robust under fixed- and random-effects. This is the quantified explanation for why ITS effect sizes
   vary so enormously, and it is unextracted from a source already cited. A large ITS effect drawn
   from nonjournal reports is close to no effect at all. Give it the prominence the evidence warrants.
2. **The relevant What Works Clearinghouse domain rating** — locate it in the cited source; report it
   accurately, including what it covers and its scope limits.
3. **Related caveats already present inside cited sources** — limitations, scope conditions, comparator
   definitions, population restrictions. Read full text, not abstracts.

## Corrections already applied — preserve, do not undo
F5's inherited ellipsis reversed Anderson's meaning (power law = a "surface contradiction" to ACT's
view, a problem to explain away, not its signature) · F4 "three-stage" → Anderson says **two** ·
F3 **g=.05 not .10** (fixed-effect column reported as random-effects) · F1 Q4→Q5 · F12 **k=2 not 3**,
so the striking −.42 is essentially one quasi-experiment and the only true experiment in that
subgroup is null (g=−0.07) · F15's fMRI results are **Qin et al. (2003)**, not Anderson's own, and
the paper attributes the speed-up to base-level declarative learning, not the declarative→procedural
transition (Q3→Q4) · "Anderson abandoned ACT\*" is Taatgen & Lee's characterisation, not Anderson's ·
the retraction over-counted "two empirical tests"; accurate count is **one**.
Three legitimate **upgrades** were also applied where the collector was too pessimistic — do not reverse.

## Calibration
A Phase 1 verifier caught the program drifting pessimistic: *"the two overstatements I found both run
pessimistic... zero kills here is not a clean bill of health."* If a claim is accurate as written,
**leave it and say so**. If a prior correction over-corrected, reverse it.

## Output
Edit in place, preserving structure and finding IDs. Corrections land **in the dossier**, not only in
a note. Mark superseded content explicitly rather than silently replacing it.
Report → `journal/raw/_inbox/foundation-audit-p1/remediation/C2-editorial-report.md`, listing every
finding ID changed AND every one examined and deliberately left unchanged.
**Do not append to the shared registers** — return rows in your response text.

Return: `Card`, `Result`, `Evidence`, `Next`, `Files`, `Blocker`.
